# ============================================
# WMS Stock Scheduler - 환경 설정 예제
# ============================================
# 이 파일을 복사하여 config.env로 이름을 변경하고 실제 값을 입력하세요
# cp config.env.example config.env

# ============================================
# 테스트 모드 설정
# ============================================
# true로 설정하면 리포트 제목에 [TEST] 접두사가 추가됩니다
TEST_MODE=false

# ============================================
# CMS URL 설정
# ============================================
# 상품코드 링크에 사용되는 CMS WMS URL
CMS_URL=http://localcms.siliconii.com

# ============================================
# 스케줄 시간 설정 (매일 실행)
# ============================================
DB_EXPORT_HOUR=8
DB_EXPORT_MINUTE=0
REPORT_HOUR=8
REPORT_MINUTE=30

# ============================================
# 로그 설정
# ============================================
LOG_LEVEL=INFO
LOG_ROTATION=1 day
LOG_RETENTION=30 days

# ============================================
# 슬랙 알림 설정
# ============================================
SEND_SLACK_NOTIFICATION=true
COMMON_API_PATH=https://your-api-server.com
SLACK_DM_RECEIVER=your-email@company.com

# ============================================
# Notion 리포트 설정
# ============================================
SEND_NOTION_REPORT=true
NOTION_API_TOKEN=ntn_your_notion_token_here
NOTION_DATABASE_ID=your_database_id_here
NOTION_PAGE_ID=your_page_id_here

# ============================================
# DB 연결 설정
# ============================================
DB_TYPE=mssql
DB_HOST=your-database-host.com
DB_PORT=1433
DB_NAME=CMSGLOBAL
DB_USER=your_username
DB_PASSWORD=your_password

# DB 연결 방식: native (pymssql) 또는 odbc (ODBC 드라이버 필요)
# ODBC 드라이버가 없는 환경에서는 native 사용 (기본값)
DB_CONNECTION_METHOD=native

# ============================================
# DB 내보내기 설정
# ============================================
# CSV 파일 저장 경로 (월별 폴더로 자동 정리됨)
# - 상대 경로: 실행 파일 기준 (예: output/daily-stock)
#   → 실제 저장: output/daily-stock/2026-02/Stock_2026-02-23_0800.csv
# - 절대 경로: 그대로 사용 (예: D:/inventory/daily-stock)
#   → 실제 저장: D:/inventory/daily-stock/2026-02/Stock_2026-02-23_0800.csv
# - 비워두면 기본값: output/daily-stock
# - 월별 폴더는 자동 생성됩니다 (yyyy-mm 형식)
DB_EXPORT_OUTPUT_DIR=output/daily-stock

# SQL 쿼리 파일 경로 (실행 파일 기준)
DB_EXPORT_SQL_FILE=repository/stock_export.sql
